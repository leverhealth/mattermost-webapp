version: 0.2

phases:
  install:
    commands:
      - echo Installing curl and nodejs
      - apt-get install -y curl
      - curl -sL https://deb.nodesource.com/setup_10.x | sudo -E bash
      - sudo apt-get install -y nodejs
      - sudo npm install -g n
      - sudo n 10.22.0
      - sudo apt update -y
      - git clone https://github.com/mattermost/mattermost-redux.git
      - cd mattermost-redux/
      - make bundle
      - cd ..
  build:
    commands:
      - echo Build starting build
      - make package
  post_build:
    commands:
      - echo Build completed
      #- echo uploading to staging ec2 instance
      #- scp mattermost-webapp.tar.gz mattermost/ec2
      #- ssh -i insert info here ec2 instance mattermost@
      #- cp mattermost-webapp.tar.gz
      #- cd /opt/mattermost
      #- tar -xvf --override mattermost-webapp.tar.gz
artifacts:
  files:
    - mattermost-webapp.tar.gz
