version: 0.2

phases:
  pre_build:
    commands:
      - echo Configuring Build Env $(date)
      -
  build:
    commands:
      - echo Build starting $(date)
      - make package
  post_build:
    commands:
      - echo Build completed $(date)
artifacts:
  files:
    - client
