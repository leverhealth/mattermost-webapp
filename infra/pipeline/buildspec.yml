version: 0.2

phases:
  install:
    commands:
      - echo Installing curl and nodejs
      - apt-get install -y curl
      - curl -sL https://deb.nodesource.com/setup_10.x | sudo -E bash
      - apt-get install -y nodejs
      - git clone https://github.com/mattermost/mattermost-redux.git
      - cd mattermost-redux/
      - make bundle
      - cd ..
  build:
    commands:
      - echo Build starting build
      - make package
  post_build:
    commands:
      - echo Build completed, starting upload
      #- scp -i .pemfileHere mattermost-webapp-tar.gz mattermost@$EC2_PRIVATE:/opt/mattermost
      #- ssh -i .pemfileHere mattermost@ec2-instance
      #- cd/opt/mattermost
      #- rm -r client
      #- tar -xvf mattermost-webapp.tar.gz
artifacts:
  files:
    - mattermost-webapp.tar.gz
